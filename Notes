What we want them to know:
two-way data-binding
$scope object
how to make a basic (with nothing) angular app
where angular is NOT a good idea!
$digest() cycle
file with embedded script for them to download/play with
